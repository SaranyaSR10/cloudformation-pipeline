AWSTemplateFormatVersion: '2010-09-09'
Description: Secure S3 bucket for CodePipeline artifacts

Resources:
  ArtifactBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault:
              SSEAlgorithm: aws:kms
      VersioningConfiguration:
        Status: Enabled
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

Outputs:
  PipelineArtifactBucketName:
    Description: Name of the Artifact S3 bucket for Pipeline
    Value: !Ref ArtifactBucket
    Export:
      Name: PipelineArtifactBucketName   

  PipelineArtifactBucketArn:
    Description: ARN of the Artifact S3 bucket for Pipeline
    Value: !GetAtt ArtifactBucket.Arn
    Export:
      Name: PipelineArtifactBucketArn   